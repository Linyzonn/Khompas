name: khompas
description: "Le compagnon de ta pr̩pa : colloscope, kh̫lles, notes et plan de travail."
publish_to: "none"
version: 0.5.0+5

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  file_picker: ^11.0.0
  http: ^1.2.0
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  share_plus: ^10.0.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
